name: Actions Smoke Test

on:
  push:
    branches:
      - sync/upstream-20260924
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "GitHub Actions is working"
